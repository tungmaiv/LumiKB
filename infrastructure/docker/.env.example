# =============================================================================
# LumiKB Docker Compose Environment Configuration
# =============================================================================
# Copy this file to .env and customize values for your environment
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker compose up -d
#
# IMPORTANT: Never commit .env to version control!
# =============================================================================

# =============================================================================
# PostgreSQL Configuration
# =============================================================================
# Primary database for users, knowledge bases, documents, permissions, audit
POSTGRES_USER=lumikb
POSTGRES_PASSWORD=lumikb_dev_password
POSTGRES_DB=lumikb
# Connection URL for backend (use 'postgres' as host when running in Docker network)
# DATABASE_URL=postgresql+asyncpg://lumikb:lumikb_dev_password@postgres:5432/lumikb

# =============================================================================
# Redis Configuration
# =============================================================================
# Used for sessions, caching, rate limiting, and Celery task broker
# No password required for development
# REDIS_URL=redis://redis:6379/0

# =============================================================================
# MinIO Configuration (S3-Compatible Object Storage)
# =============================================================================
# Used for document file storage
MINIO_ROOT_USER=lumikb
MINIO_ROOT_PASSWORD=lumikb_dev_password
# MinIO endpoint for backend (use 'minio' as host when running in Docker network)
# MINIO_ENDPOINT=http://minio:9000
# MINIO_ACCESS_KEY=lumikb
# MINIO_SECRET_KEY=lumikb_dev_password
# MINIO_BUCKET=lumikb-documents

# =============================================================================
# Qdrant Configuration (Vector Database)
# =============================================================================
# Used for semantic search and document embeddings
# QDRANT_HOST=qdrant
# QDRANT_PORT=6333
# QDRANT_GRPC_PORT=6334
# QDRANT_COLLECTION=lumikb_documents

# =============================================================================
# LiteLLM Configuration (LLM Gateway)
# =============================================================================
# Used for unified LLM API access with model routing
LITELLM_MASTER_KEY=sk-dev-master-key

# LLM Provider API Keys (add your keys here)
# At least one is required for LLM functionality
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: Additional provider keys
# AZURE_API_KEY=
# AZURE_API_BASE=
# COHERE_API_KEY=
# HUGGINGFACE_API_KEY=

# =============================================================================
# Backend Application Configuration (for reference)
# =============================================================================
# These are typically set in backend/.env, not here
# LUMIKB_DATABASE_URL=postgresql+asyncpg://lumikb:lumikb_dev_password@localhost:5432/lumikb
# LUMIKB_REDIS_URL=redis://localhost:6379/0
# LUMIKB_MINIO_ENDPOINT=http://localhost:9000
# LUMIKB_QDRANT_HOST=localhost
# LUMIKB_QDRANT_PORT=6333
# LUMIKB_LITELLM_URL=http://localhost:4000
# LUMIKB_SECRET_KEY=your-secret-key-here

# =============================================================================
# Development Settings
# =============================================================================
# Set to 'true' for verbose logging
# DEBUG=false
